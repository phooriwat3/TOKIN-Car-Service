'use client';
import Link from 'next/link';
import { CalendarDays, Clock, MapPin, Users } from 'lucide-react';
import { useApp } from './app-provider';
import { Badge, Button, Card, Empty } from './ui';
import { statusLabel, totalCost } from '@/lib/business';
export function BookingDetail({ id, admin = false }: { id: string; admin?: boolean }) { const { data } = useApp(); const b = data.bookings.find(x => x.id === id); if (!b) return <Empty title="Booking not found" body="This record does not exist in the demo dataset." />; const vehicle = data.vehicles.find(x => x.id === b.assignment?.vehicleId); const driver = data.drivers.find(x => x.id === b.assignment?.driverId); return <div className="space-y-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm text-gray-500">{b.bookingNo}</p><h1 className="text-2xl font-bold">{b.destination}</h1></div><Badge status={b.status}>{statusLabel(b.status)}</Badge></div><div className="grid gap-5 lg:grid-cols-3"><Card className="p-5 lg:col-span-2"><h2 className="mb-4 font-bold">Trip request</h2><div className="grid gap-5 sm:grid-cols-2"><Info icon={<CalendarDays />} label="Date" value={b.usingDate} /><Info icon={<Clock />} label="Time" value={`${b.startTime} - ${b.endTime}`} /><Info icon={<MapPin />} label="Route" value={`${b.pickupLocation} to ${b.destination}`} /><Info icon={<Users />} label="Passengers" value={String(b.numPassengers)} /></div><div className="mt-5 border-t border-line pt-5"><p className="text-xs font-semibold uppercase text-gray-500">Purpose</p><p className="mt-1 text-sm">{b.purpose}</p></div>{b.urgent && <div className="mt-4 rounded-md border border-amber-200 bg-amber-50 p-3 text-sm"><strong>Urgent:</strong> {b.urgentReason}</div>}</Card><Card className="p-5"><h2 className="mb-4 font-bold">Request details</h2><Rows items={[['Requester', b.requesterName], ['Department', b.department], ['Category', statusLabel(b.category)], ['Vehicle preference', statusLabel(b.vehicleTypePref)], ['Meeting point', statusLabel(b.meetingPoint)]]} /></Card></div>{b.approval && <Card className="p-5"><h2 className="font-bold">Approval decision</h2><p className="mt-2 text-sm">{b.approval.comments}</p><p className="mt-1 text-xs text-gray-500">{b.approval.approverName} · {new Date(b.approval.actedAt).toLocaleString()}</p></Card>}{b.assignment && <Card className="p-5"><div className="flex justify-between"><h2 className="font-bold">Assignment</h2>{admin && <Link className="text-sm font-semibold text-brand" href={`/admin/bookings/${b.id}`}>Edit assignment</Link>}</div><div className="mt-4 grid gap-4 sm:grid-cols-2"><Rows items={[['Vehicle', vehicle ? `${vehicle.licensePlate} · ${vehicle.brand} ${vehicle.model}` : 'Unknown'], ['Driver', driver?.fullName || 'Unknown']]} /><Rows items={[['Driver status', b.assignment.accepted ? 'Accepted' : 'Awaiting acceptance'], ['Assigned', new Date(b.assignment.assignedAt).toLocaleString()]]} /></div></Card>}{b.tripLog && <Card className="p-5"><h2 className="mb-3 font-bold">Trip log</h2><div className="grid gap-4 sm:grid-cols-3"><Rows items={[['Departure', b.tripLog.actualTimeOut ? new Date(b.tripLog.actualTimeOut).toLocaleString() : '-'], ['Return', b.tripLog.actualTimeIn ? new Date(b.tripLog.actualTimeIn).toLocaleString() : '-']]} /><Rows items={[['Starting mileage', b.tripLog.startMileage?.toString() || '-'], ['Finished mileage', b.tripLog.endMileage?.toString() || '-']]} /><Rows items={[['Total expenses', `THB ${totalCost(b).toLocaleString()}`], ['Remarks', b.tripLog.remarks || '-']]} /></div></Card>}</div> }
function Info({ icon, label, value }: { icon: React.ReactNode; label: string; value: string }) { return <div className="flex gap-3 text-sm"><span className="text-brand [&>svg]:h-5 [&>svg]:w-5">{icon}</span><div><p className="text-xs text-gray-500">{label}</p><p className="font-medium">{value}</p></div></div> }; function Rows({ items }: { items: string[][] }) { return <dl className="space-y-3">{items.map(([a, b]) => <div key={a}><dt className="text-xs text-gray-500">{a}</dt><dd className="text-sm font-medium">{b}</dd></div>)}</dl> }
