'use client';import Link from 'next/link';import { useMemo,useState } from 'react';import { addDays,format,startOfWeek } from 'date-fns';import { useApp } from '@/components/app-provider';import { Badge,Button,Card } from '@/components/ui';import { PageHeader } from '@/components/page-header';import { ChevronLeft,ChevronRight } from 'lucide-react';export default function Calendar(){const {data}=useApp();const [offset,setOffset]=useState(0);const days=useMemo(()=>{const s=addDays(startOfWeek(new Date(),{weekStartsOn:1}),offset*7);return Array.from({length:7},(_,i)=>addDays(s,i))},[offset]);return <><PageHeader title="Booking schedule" description="Weekly fleet schedule with assignment status." action={<div className="flex gap-2"><Button variant="secondary" onClick={()=>setOffset(x=>x-1)}><ChevronLeft size={17}/></Button><Button variant="secondary" onClick={()=>setOffset(0)}>This week</Button><Button variant="secondary" onClick={()=>setOffset(x=>x+1)}><ChevronRight size={17}/></Button></div>}/><div className="grid gap-px overflow-hidden rounded-lg border border-line bg-line md:grid-cols-7">{days.map(d=>{const ds=format(d,'yyyy-MM-dd'),items=data.bookings.filter(b=>b.usingDate===ds);return <div key={ds} className="min-h-40 bg-white p-3"><div className="mb-3"><p className="text-xs font-semibold uppercase text-gray-500">{format(d,'EEE')}</p><p className="text-xl font-bold">{format(d,'d')}</p></div><div className="space-y-2">{items.map(b=><Link key={b.id} href={`/admin/bookings/${b.id}`} className="block rounded-md border border-line p-2 hover:border-brand"><p className="text-xs font-semibold">{b.startTime} · {b.destination}</p><div className="mt-1"><Badge status={b.status}>{b.status.replace('_',' ')}</Badge></div></Link>)}</div></div>})}</div></>}
