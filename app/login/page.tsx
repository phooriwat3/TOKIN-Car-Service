'use client';

import { CarFront, ShieldCheck } from 'lucide-react';
import { Role } from '@/lib/types';
import { useApp } from '@/components/app-provider';
import { Button, Card } from '@/components/ui';
export default function Login() { const { setRole } = useApp(); const enter = (r: Role) => { setRole(r); window.location.assign('/dashboard') }; return <main className="grid min-h-screen place-items-center bg-[#17345f] p-5"><div className="w-full max-w-3xl"><div className="mb-7 flex items-center justify-center gap-3 text-white"><div className="grid h-12 w-12 place-items-center rounded-lg bg-white text-brand"><CarFront /></div><div><h1 className="text-2xl font-bold">Car Service Requisition System</h1><p className="text-sm text-blue-200">TOKIN operations demo</p></div></div><Card className="p-6 md:p-8"><div className="mb-6 flex items-start gap-3"><ShieldCheck className="text-brand" /><div><h2 className="text-lg font-bold">Choose a demo role</h2><p className="text-sm text-gray-500">No account or Supabase connection is required for this MVP.</p></div></div><div className="grid gap-3 sm:grid-cols-2">{(['requester', 'approver', 'admin', 'driver'] as Role[]).map(r => <button type="button" key={r} onClick={() => enter(r)} className="rounded-md border border-line p-4 text-left hover:border-brand hover:bg-blue-50"><span className="font-semibold capitalize">{r}</span><span className="mt-1 block text-xs text-gray-500">Open the {r} workspace</span></button>)}</div><Button type="button" className="mt-6 w-full" onClick={() => enter('requester')}>Continue as requester</Button></Card></div></main> }
